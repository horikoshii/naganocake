<div class="container">
 <div class="row">
  <div class="col-lg-2" style="background-color: orange;">

   <thead>
    <tr>ジャンル検索</tr>
   </thead>

   <tbody>
    <% @genres.each do |genre| %>
    <p><%= link_to genre.name, serch_path(genre_id: genre.id) %></p>
    <% end %>
   </tbody>
  </div>

  <div class="col-lg-10">
   <table class="table table-striped">

    <thead>
     <tr>
      <th></th>
     </tr>
    </thead>

    <tbody>
     <tr>
      <th>
       <h2>
        ようこそ、ながのCAKEへ<br>
        このサイトは、ケーキ販売のECサイトになります<br>
        会員でない方も閲覧はできますが、<br>
        購入には会員登録が必要になります
       </h2>
      </th>
     </tr>
    </tbody>
   </table>

   <div class="row">
    <h4><strong>新着商品</strong></h4>
   </div>
   <div class="row">
    <% @items.each do |item| %>

    <div class="col-lg-3">
     <p><%= image_tag item.image, size: "200x200" %></p>
     <p><%= link_to item.name,item_path(item.id) %></p>
     <p><%= item.price %></p>
    </div>

    <% end %>
   </div>
  </div>
 </div>
</div>
